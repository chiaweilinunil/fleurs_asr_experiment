# results/

- `predictions/` — per-experiment JSONL (one record per utterance).
- `metrics/` — CSV/JSON metric summaries with full provenance.

Contents are git-ignored except these READMEs/.gitkeep files.
