"""Shared helpers: config loading, logging, seeding, git/hardware metadata.

Responsibilities (to be implemented):
    * load YAML configs into typed objects
    * configure ``rich``/``logging`` consistently
    * set deterministic seeds
    * capture git commit hash and basic hardware info for provenance
"""

# TODO: implement load_config / setup_logging / set_seed / get_git_commit.
