"""fleurs_asr: reusable library for low-resource ASR experiments on FLEURS.

This package holds all reusable logic (data loading, text normalization,
evaluation, decoding, error analysis, reporting). Command-line entry points
live in ``scripts/`` and should stay thin wrappers around this package.
"""

__version__ = "0.0.0"
